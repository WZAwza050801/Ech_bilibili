# 复制为 .env 后填写；.env 已被 .gitignore 拦截，永不入库
# 各 Key 申请方式与选型理由见 docs/API_SETUP.md
# 部署与网络（反向隧道 / 配额 / 网盘坑）必读见 docs/DEPLOY.md
#
# 说明：这里全部是【可选】变量，不填会走合理默认值（路径按脚本位置推导）。
#       只有换盘、换机器、上服务器、或要用代理时才需要动。

# ── LLM 密钥 ────────────────────────────────────────────────────────────────
# 口播稿 polish 格式整理用；不配则跳过润色（管线仍能出笔记）
DEEPSEEK_API_KEY=
# 也可换端点（如自建代理 / 兼容 OpenAI 协议的服务）
# DEEPSEEK_BASE_URL=https://api.deepseek.com

# ── ⚠️ YouTube 网络（不配代理在国内服务器上一定跑不通）────────────────────
# 国内服务器 curl youtube.com 会【直接超时】（国际出口受限）；
# "服务器能 pip install" ≠ "服务器能上 YouTube"。三种情形区别对待：
#
#   1) 本机（Windows/macOS，开着梯子）
#        YT_PROXY=http://127.0.0.1:12000
#
#   2) 国内服务器（本项目实测路线：SSH 反向隧道借本机梯子）
#        先在本机执行（跑批期间保持开着，别关本机、别关梯子）：
#            ssh -N -R 17890:127.0.0.1:12000 <user>@<server>
#        再在服务器上填：
#        YT_PROXY=http://127.0.0.1:17890
#        验证：curl -sI --max-time 15 -x http://127.0.0.1:17890 https://www.youtube.com
#             期望 HTTP/2 200；若返回 501 Unsupported method ('CONNECT') 说明端口被哑服务占了，不是通了
#
#   3) 海外 VPS（本来就能直连）
#        YT_PROXY=direct
#
# YT_PROXY=
# 别名：ECHONOTES_YT_PROXY（主推）> ECH_YT_PROXY > YT_PROXY，设哪个都认，前者优先

# 注意：ech_config.py 会自动把 LLM 域名（api.deepseek.com）写进 NO_PROXY，
#       即【LLM 调用绕开梯子走直连】——否则隧道一挂，连润色都做不了。
#       所以梯子只负责 YouTube/yt-dlp，不影响 LLM。

# ── 路径（默认按脚本位置推导，一般不用填）──────────────────────────────────
# 换盘 / 换机器 / 服务器目录结构与本机不同时才需要
# 命名同样是 ECHONOTES_*（主推）/ ECH_*（别名）双兼容
# ECHONOTES_ROOT / ECH_ROOT=
# ECHONOTES_YT_DIR / ECH_YT_DIR=
# ECHONOTES_BILI_DIR / ECH_BILI_DIR=
# whisper 模型目录（两平台共享同一份，换机 scp 拷一次即可）
# ECHONOTES_ASR_MODEL / ECH_MODEL_DIR=
# ASR 独立虚拟环境的 python（用 requirements-asr.txt 隔离装法时指向它）
# ECHONOTES_ASR_PYTHON / ECH_PY=

# ── 系统二进制路径（没进 PATH 时用）────────────────────────────────────────
# ffmpeg 完整路径；例如 Windows: FFMPEG=C:\ffmpeg\bin\ffmpeg.exe
# FFMPEG=

# ── 本机密钥文件（可选，仓库里永远没有真实密钥）────────────────────────────
# 不设 DEEPSEEK_API_KEY 时从这里回退读取
# ECHONOTES_SECRETS_FILE / ECH_SECRETS=
